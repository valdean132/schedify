<a href="{{ $href }}" 
    class="p-2 m-1 rounded-3 bg-sec" 
    data-bs-toggle="tooltip" 
    data-bs-title="Autenticar com o {{ $alt }}" 
    data-bs-placement="bottom">
    <img src="{{ $src }}" 
        alt="{{ $alt }}" 
        width="{{ $size }}" 
        height="{{ $size }}">
</a>